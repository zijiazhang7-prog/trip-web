# 团队联调待办清单

> 本轮已完成除 **AIGC 旅游动画** 外的数据/功能要求前后端接线。以下事项需与后端、产品或运维同学确认后推进。

## 一、需后端配合（P0）

| # | 事项 | 前端状态 | 后端状态 | 验收方式 |
|---|------|----------|----------|----------|
| 1 | Taxonomy 推荐加权 | 已传 `destType` / `interestTags` / `cuisineTags` | `TaxonomyService` + `RecommendServiceImpl` / `FoodServiceImpl` 已实现 | 登录用户选「古镇」等标签，推荐排序与前端一致 |
| 2 | `GET /api/v1/diaries/recommend?sortBy=interest` | 社群「兴趣推荐」已接线 | 已实现 | 登录后切换兴趣推荐，结果随偏好变化 |
| 3 | `GET /api/v1/diaries/me` | 书架列表合并已发布日记 | 已实现，需登录 | 登录后社群选手账可见已发布项 |
| 4 | `GET /api/v1/facilities/search` | 导航页关键字/类型搜索已接线 | 已实现 | 景区内输入关键字返回图上设施 |
| 5 | `GET /api/v1/routes/history` | 导航页展示最近 5 条 | 已实现，需登录 | 规划路线后历史列表有记录 |

## 二、需后续迭代（P1）

| # | 事项 | 说明 |
|---|------|------|
| 1 | **AIGC 旅游动画** | 等负责人提供 API Key / 接口文档后接入 `RouteSketch` 与动画生成 |
| 2 | `GET /api/v1/taxonomy` 公开接口 | 当前前后端各加载本地 `taxonomy.json`；若需热更新可补 REST 出口 |
| 3 | 用户偏好字段扩展 | 后端 `UserPreferenceVO` 仅 `preferThemeList` / `preferFoodType`；新兴趣标签完整落库需 DB 迁移 |
| 4 | 室内导航深化 | 景区 SVG / 楼层图与 BLE 定位属加分项，当前为图节点 + 高德回退 |
| 5 | `foods/feed` 聚合接口 | 美食页现为分批 `recommend`；若需单接口 Feed 可后端聚合 |
| 6 | 正式课程设计报告 | 按《数据结构课程设计评分原则-2026.pdf》撰写 Word/PDF 报告 |

## 三、联调环境

- 前端：`c:\Users\Lenovo\Desktop\trip\cursor`，分支 `fix/integration-feedback`
- 后端：`c:\Users\Lenovo\Desktop\workspace\trip-web`，分支 `dev`
- 代理：确认 `VITE_API_BASE` 指向后端；JWT 登录后测 recommend / diaries/me / routes/history

## 四、本轮已跳过

- AIGC 视频/路线手绘生成（明确等 API）
- 管理端 / 导入脚本改动（非课程演示必需）

---

*生成时间：2026-06-12 · 可与 `docs/backend-标签体系与个性化推荐任务.md` §7 验收用例对照测试。*
